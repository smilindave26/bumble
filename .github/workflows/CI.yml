name: CI
on: [ push ]

jobs:
  ziti-action:
    runs-on: macos-latest
    name: Test Ziti Action
    steps:
    - name: Ziti Action 
      id: ziti-action
      uses: smilindave26/ziti-test-action@main
      with:
        ziti-id: ${{ secrets.ZITI_WEBHOOK_ACTION_ID }}
        webhook-url: 'https://ziti-action.openziti.org:80/inspect/01ezdtzcnrp6tyaeq03v68t7hf/ziti'
        webhook-secret: 'todo-get-from-secrets'
      env:
        ZITI_NODEJS_LOG: 11
